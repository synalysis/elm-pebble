Vendored from lamdera/codecs 1.0.0 (BSD-3-Clause).

elm-pages generated Main imports Lamdera.Wire3. The package is not on
package.elm-lang.org; elm-pages injects it via the Lamdera compiler. elmc
loads these sources through elm.json source-directories.
