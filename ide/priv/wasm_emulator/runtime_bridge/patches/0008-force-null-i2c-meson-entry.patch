diff --git a/build.sh b/build.sh
index 80851aa..5f3b4e7 100755
--- a/build.sh
+++ b/build.sh
@@ -113,6 +113,27 @@ patch_meson "${QEMU_SRC}/hw/misc/meson.build" "stm32_pebble" \
   'pebble_null_input.c',
 ))"
 
+if ! grep -q "pebble_null_i2c.c" "${QEMU_SRC}/hw/misc/meson.build"; then
+    python3 - "${QEMU_SRC}/hw/misc/meson.build" <<'PY'
+import pathlib, sys
+path = pathlib.Path(sys.argv[1])
+path.write_text(path.read_text().replace("'stm32_pebble_i2c.c',", "'stm32_pebble_i2c.c',\n  'pebble_null_i2c.c',"))
+PY
+fi
+
+if ! grep -q "pebble_null_input.c" "${QEMU_SRC}/hw/misc/meson.build"; then
+    python3 - "${QEMU_SRC}/hw/misc/meson.build" <<'PY'
+import pathlib, sys
+path = pathlib.Path(sys.argv[1])
+source = path.read_text()
+if "'pebble_null_i2c.c'," in source:
+    source = source.replace("'pebble_null_i2c.c',", "'pebble_null_i2c.c',\n  'pebble_null_input.c',")
+else:
+    source = source.replace("'stm32_pebble_i2c.c',", "'stm32_pebble_i2c.c',\n  'pebble_null_input.c',")
+path.write_text(source)
+PY
+fi
+
 # hw/timer/meson.build - timers AND RTC (RTC is in timer dir)
 patch_meson "${QEMU_SRC}/hw/timer/meson.build" "stm32_pebble" \
 "system_ss.add(when: 'CONFIG_PEBBLE', if_true: files(
diff --git a/build_wasm.sh b/build_wasm.sh
index 33d1f53..f4e1d34 100644
--- a/build_wasm.sh
+++ b/build_wasm.sh
@@ -153,6 +153,27 @@ patch_meson hw/misc/meson.build "stm32_pebble" \
   '"'"'pebble_null_input.c'"'"',
 ))"
 
+if ! grep -q "pebble_null_i2c.c" hw/misc/meson.build; then
+    python3 - hw/misc/meson.build <<'"'"'PY'"'"'
+import pathlib, sys
+path = pathlib.Path(sys.argv[1])
+path.write_text(path.read_text().replace("'"'"'stm32_pebble_i2c.c'"'"',", "'"'"'stm32_pebble_i2c.c'"'"',\n  '"'"'pebble_null_i2c.c'"'"',"))
+PY
+fi
+
+if ! grep -q "pebble_null_input.c" hw/misc/meson.build; then
+    python3 - hw/misc/meson.build <<'"'"'PY'"'"'
+import pathlib, sys
+path = pathlib.Path(sys.argv[1])
+source = path.read_text()
+if "'"'"'pebble_null_i2c.c'"'"'," in source:
+    source = source.replace("'"'"'pebble_null_i2c.c'"'"',", "'"'"'pebble_null_i2c.c'"'"',\n  '"'"'pebble_null_input.c'"'"',")
+else:
+    source = source.replace("'"'"'stm32_pebble_i2c.c'"'"',", "'"'"'stm32_pebble_i2c.c'"'"',\n  '"'"'pebble_null_input.c'"'"',")
+path.write_text(source)
+PY
+fi
+
 # hw/timer/meson.build
 patch_meson hw/timer/meson.build "stm32_pebble" \
 "system_ss.add(when: '"'"'CONFIG_PEBBLE'"'"', if_true: files(
diff --git a/build_wasm_jit.sh b/build_wasm_jit.sh
index a99db72..1cf2d10 100755
--- a/build_wasm_jit.sh
+++ b/build_wasm_jit.sh
@@ -161,6 +161,27 @@ patch_meson hw/misc/meson.build "stm32_pebble" \
   '"'"'pebble_null_input.c'"'"',
 ))"
 
+if ! grep -q "pebble_null_i2c.c" hw/misc/meson.build; then
+    python3 - hw/misc/meson.build <<'"'"'PY'"'"'
+import pathlib, sys
+path = pathlib.Path(sys.argv[1])
+path.write_text(path.read_text().replace("'"'"'stm32_pebble_i2c.c'"'"',", "'"'"'stm32_pebble_i2c.c'"'"',\n  '"'"'pebble_null_i2c.c'"'"',"))
+PY
+fi
+
+if ! grep -q "pebble_null_input.c" hw/misc/meson.build; then
+    python3 - hw/misc/meson.build <<'"'"'PY'"'"'
+import pathlib, sys
+path = pathlib.Path(sys.argv[1])
+source = path.read_text()
+if "'"'"'pebble_null_i2c.c'"'"'," in source:
+    source = source.replace("'"'"'pebble_null_i2c.c'"'"',", "'"'"'pebble_null_i2c.c'"'"',\n  '"'"'pebble_null_input.c'"'"',")
+else:
+    source = source.replace("'"'"'stm32_pebble_i2c.c'"'"',", "'"'"'stm32_pebble_i2c.c'"'"',\n  '"'"'pebble_null_input.c'"'"',")
+path.write_text(source)
+PY
+fi
+
 # hw/timer/meson.build
 patch_meson hw/timer/meson.build "stm32_pebble" \
 "system_ss.add(when: '"'"'CONFIG_PEBBLE'"'"', if_true: files(
